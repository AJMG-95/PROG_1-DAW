// Crear un array con los 5 primeros numeros pares

public class Pares {
    public static void main(String[] args) {
        int[] a = new int[5]; // Crea un array de 5 elementos de tipo int

        // a[0] = 2;
        // a[1] = 4;
        // a[2] = 6;
        // a[3] = 8;
        // a[4] = 10;

        // for (int i = 0; i < a.length; i++) {
        // a[i] = (i + 1) * 2;
        // }

        a = new int[] { 2, 4, 6, 8, 10 };

    }
}
